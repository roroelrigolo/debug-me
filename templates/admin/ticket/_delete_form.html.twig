<form method="post" action="{{ path('app_admin_ticket_delete', {'id': ticket.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer le ticket : {{ ticket.title }} ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ticket.id) }}">
    {% include 'assets/button/btn_black.html.twig' with {'tag': 'button', 'text': "Supprimer le ticket"} %}
</form>
