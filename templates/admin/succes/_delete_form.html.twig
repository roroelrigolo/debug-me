<form method="post" action="{{ path('app_admin_succes_delete', {'id': succes.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer le succès : {{ succes.name }} ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ succes.id) }}">
    {% include 'components/button/_btn_black.html.twig' with {'tag': 'button', 'text': "Supprimer le succès"} %}
</form>
